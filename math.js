const e = Math.E;
const piValue = Math.PI;

const number = 2.234;

const abs = Math.abs(number);

const floorValue = Math.floor(number);
const ceilValue = Math.ceil(number);
const roundValue = Math.round(number);

const mathMin = Math.min(200, 100, 50, 300);

const mathMax = Math.max(200, 100, 490, 200);

const numberPower = Math.pow(5, 2);

const squerRoot = Math.sqrt(64);

const randomNumber = Math.random();

console.log('randomNumber:', randomNumber);
