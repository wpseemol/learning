import UploadComponent from './_component/upload-component';

export default function UploadPage() {
    return (
        <main className="main flex justify-center items-center h-screen">
            <UploadComponent />
        </main>
    );
}
